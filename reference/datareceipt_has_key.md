# Is an API key available?

Is an API key available?

## Usage

``` r
datareceipt_has_key()
```

## Value

`TRUE` if a key is set in the `DATARECEIPT_API_KEY` environment
variable, `FALSE` otherwise.

## Examples

``` r
datareceipt_has_key()
#> [1] FALSE
```
