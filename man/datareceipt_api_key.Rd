% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/key.R
\name{datareceipt_api_key}
\alias{datareceipt_api_key}
\title{Set your Data Receipt API key}
\usage{
datareceipt_api_key(key, install = FALSE, overwrite = FALSE)
}
\arguments{
\item{key}{Your token, as shown once when you created it. It looks like
\code{"12|dr_..."}.}

\item{install}{If \code{TRUE}, save the key to \code{.Renviron} for future sessions.
If \code{FALSE} (the default), set it for the current session only.}

\item{overwrite}{If \code{TRUE}, replace a key already saved in \code{.Renviron}.}
}
\value{
The key, invisibly.
}
\description{
Data Receipt identifies you by an API token, which you create on the site
under Settings > API tokens. Every function in this package looks for it in
the \code{DATARECEIPT_API_KEY} environment variable, or takes it directly as a
\code{key} argument.
}
\details{
With \code{install = TRUE} the key is written to your \code{.Renviron} file so it is
picked up in every future R session, the same way
\code{tidycensus::census_api_key()} works. Any existing \code{.Renviron} is backed up
to \code{.Renviron_backup} first.
}
\examples{
\dontrun{
# Once, on each computer you use:
datareceipt_api_key("12|dr_...", install = TRUE)

# Or for this session only:
datareceipt_api_key("12|dr_...")
}
}
