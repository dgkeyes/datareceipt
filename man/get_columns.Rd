% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/requests.R
\name{get_columns}
\alias{get_columns}
\title{Get a request's columns}
\usage{
get_columns(request, key = NULL)
}
\arguments{
\item{request}{A request id (\code{12}), or the request's URL on the site
(\code{"https://datareceipt.io/requests/12"}), or one row of \code{\link[=list_requests]{list_requests()}}.}

\item{key}{Your API key. Defaults to the \code{DATARECEIPT_API_KEY} environment
variable; see \code{\link[=datareceipt_api_key]{datareceipt_api_key()}}.}
}
\value{
A tibble with one row per column: \code{name} (the variable name the
data is keyed by), \code{type} (one of \code{"text"}, \code{"numeric"}, \code{"integer"},
\code{"date"}, \code{"boolean"}), \code{required} (no empty cells allowed), \code{min}, \code{max},
\code{allowed_values} (a list column of the permitted values, empty when there
is no such rule), \code{on_break} (\code{"block"} if a value breaking a rule stops
the submission, \code{"flag"} if it is accepted and flagged; see
\code{\link[=get_flags]{get_flags()}}), \code{friendly_name} (what the data dictionary shows people, \code{NA} when
the variable name is used), and \code{description} (the explanation shown to
senders, \code{NA} when there is none).
}
\description{
The spec a request was built with: each column's name, type, and rules.
This is what the data you get back from \code{\link[=get_data]{get_data()}} is typed by, so it is
the place to look before deciding what to do with the data.
}
\examples{
\dontrun{
get_columns(12)
}
}
