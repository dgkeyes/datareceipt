% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submissions.R
\name{list_submissions}
\alias{list_submissions}
\title{List a request's submissions}
\usage{
list_submissions(request, key = NULL)
}
\arguments{
\item{request}{A request id (\code{12}), or the request's URL on the site
(\code{"https://datareceipt.io/requests/12"}), or one row of \code{\link[=list_requests]{list_requests()}}.}

\item{key}{Your API key. Defaults to the \code{DATARECEIPT_API_KEY} environment
variable; see \code{\link[=datareceipt_api_key]{datareceipt_api_key()}}.}
}
\value{
A tibble with one row per submission: \code{id}, \code{request_id},
\code{sender_name}, \code{sender_email}, \code{source} (how the data came in: \code{"xlsx"},
\code{"csv"}, \code{"paste"}, \code{"typed"}, or \code{"form"}), \code{row_count}, \code{flag_count}
(cells that broke a rule but were accepted; see \code{\link[=get_flags]{get_flags()}}),
\code{original_filename}, \code{submitted_at}, and \code{revised_at} (when the request's
owner last edited the submission's data on the site, or \code{NA} if it is as
received).
}
\description{
Who sent what, and when, oldest first. The rows themselves come from
\code{\link[=get_data]{get_data()}} (all submissions at once) or \code{\link[=get_submission]{get_submission()}} (one).
}
\examples{
\dontrun{
list_submissions(12)
}
}
