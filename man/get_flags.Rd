% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flags.R
\name{get_flags}
\alias{get_flags}
\title{List the flagged cells of a request}
\usage{
get_flags(request, key = NULL)
}
\arguments{
\item{request}{A request id (\code{12}), or the request's URL on the site
(\code{"https://datareceipt.io/requests/12"}), or one row of \code{\link[=list_requests]{list_requests()}}.}

\item{key}{Your API key. Defaults to the \code{DATARECEIPT_API_KEY} environment
variable; see \code{\link[=datareceipt_api_key]{datareceipt_api_key()}}.}
}
\value{
A tibble with one row per flagged cell: \code{submission_id}, \code{row} (the
row's position within its submission), \code{submitted_at}, \code{sender_name},
\code{sender_email}, \code{column}, \code{value} (the text as sent), and \code{message} (the
rule it broke). Zero rows when nothing is flagged.
}
\description{
A request's owner can set a column to accept a value that breaks its rules
and flag it, rather than block the whole submission. This returns every
such cell across every submission: where it is, what the sender typed, and
which rule it broke. The same cells appear in \code{\link[=get_data]{get_data()}} cast to the
column's type where the text allows it, and as \code{NA} where it does not.
}
\examples{
\dontrun{
get_flags(12)
}
}
